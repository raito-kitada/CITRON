<div >
<img class="topback" srcset="{{'topback.png' | asset_url }}" style="position:absolute;right:4%;width:10%;z-index:10;"  >
</div>
<script>
/*
  const obj1 = {name:"apple",color:"red",type:"fruit"};
  $.each(obj1, function(index, value) {
  console.log(index + '= ' + value);
  })
  console.log('obj=%o',obj1);



  $('.scroll').css('top',scrollarrow_first);
  const topback = document.querySelector('.topback')
  $(window).scroll(function() {

    var top = $(this).scrollTop();
    var scrollarrow = this.innerHeight  + top - this.innerHeight*0.5;
    if(scrollarrow < top_img.offset().top+top_img.height()*0.6 ){
      $('.scroll').css('top',top_img.offset().top+top_img.height()*0.6);
    }
    else{
      // windowがスクロールされた時に実行する処理、元のトップからいくら、スクロールされている画面か
      $('.scroll').css('top',scrollarrow);
    }
    });
    */
</script>
